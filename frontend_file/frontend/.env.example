# Backend API base URL (no trailing slash).
# Next.js uses this for rewrites (proxy) and server-side requests.
# In the browser, the app uses same-origin /api/v1 so rewrites proxy to this URL.
# GitHub Codespaces: set to your forwarded backend URL, e.g. https://xxx-8000.preview.app.github.dev
NEXT_PUBLIC_API_URL=http://localhost:8000
# VITE_API_BASE_URL=http://localhost:8000
